{
  "name": "@code-paste/contracts-ci",
  "version": "1.0.0",
  "private": true,
  "description": "CI tooling for `main`: proves the contract vectors + schemas are internally consistent. Not product code.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node validate-contracts.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
